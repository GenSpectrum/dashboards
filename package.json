{
  "name": "covspectrumdashboards",
  "private": true,
  "version": "0.0.1",
  "description": "Utility for the dashboards repository",
  "type": "module",
  "devDependencies": {
    "@commitlint/cli": "^19.4.0",
    "@commitlint/config-conventional": "^19.2.2",
    "@commitlint/types": "^19.0.3"
  },
  "scripts": {
    "commitlint:last-commit": "commitlint --from=HEAD~1 --verbose",
    "commitlint:merge-base": "commitlint --from=$(git merge-base HEAD main) --verbose"
  },
  "dependencies": {
    "@genspectrum/dashboard-components": "^1.10.0"
  }
}
