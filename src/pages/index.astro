---
import BaseLayout from '../layouts/base/BaseLayout.astro';
import { LocationTimeVariantFilter } from '../components/LocationTimeVariantFilter';
import { parseUrl } from '../routing';

const routeData = parseUrl(Astro.url);
const lapisLocation = {
    region: routeData.region,
    country: routeData.country,
    division: routeData.division,
};

let subDivisionField = undefined;
let subDivisionLabel = '';
if (lapisLocation.country === undefined) {
    subDivisionField = 'country';
    subDivisionLabel = 'Countries';
} else if (lapisLocation.division === undefined) {
    subDivisionField = 'division';
    subDivisionLabel = 'Geographic sub-divisions';
}

const sizeAttribute = JSON.stringify({ width: '100%', height: '100%' });
---

<style>
    .component-title {
        @apply font-bold;
    }
</style>

<BaseLayout title='GenSpectrum'>
    <gs-app lapis='https://lapis.cov-spectrum.org/open/v2'>
        <div class='p-2' style='display: grid; grid-template-columns: 300px 1fr; grid-column-gap: 16px;'>
            <LocationTimeVariantFilter initialLocation={lapisLocation} client:only='react' />
            <div
                style='display: grid; grid-template-columns: repeat(auto-fit, minmax(min(100vw, 650px), 1fr)); grid-column-gap: 16px; grid-row-gap: 24px;'
            >
                <div>
                    <h2 class='component-title'>Prevalence over time</h2>
                    <div class='h-[400px]'>
                        <gs-prevalence-over-time
                            headline=''
                            numerator={JSON.stringify({
                                displayName: '',
                                ...lapisLocation,
                            })}
                            denominator='{}'
                            granularity='month'
                            smoothingWindow='0'
                            size={sizeAttribute}></gs-prevalence-over-time>
                    </div>
                </div>
                <div>
                    <h2 class='component-title'>Relative growth advantage</h2>
                    <div class='h-[400px]'>
                        <gs-relative-growth-advantage
                            headline=''
                            numerator={JSON.stringify(lapisLocation)}
                            denominator='{}'
                            generationTime='7'
                            size={sizeAttribute}></gs-relative-growth-advantage>
                    </div>
                </div>

                <div>
                    <h2 class='component-title'>Nucleotide mutations</h2>
                    <div class='h-[400px]'>
                        <gs-mutations
                            variant={JSON.stringify(lapisLocation)}
                            headline=''
                            sequenceType='nucleotide'
                            views='["grid", "table", "insertions"]'
                            size={sizeAttribute}></gs-mutations>
                    </div>
                </div>
                <div>
                    <h2 class='component-title'>Amino acid mutations</h2>
                    <div class='h-[400px]'>
                        <gs-mutations
                            variant={JSON.stringify(lapisLocation)}
                            headline=''
                            sequenceType='amino acid'
                            views='["grid", "table", "insertions"]'
                            size={sizeAttribute}></gs-mutations>
                    </div>
                </div>

                <div>
                    <h2 class='component-title'>Lineages</h2>
                    <div class='h-[400px]'>
                        <gs-aggregate
                            fields='["nextcladePangoLineage"]'
                            filter={JSON.stringify(lapisLocation)}
                            headline=''
                            size={sizeAttribute}></gs-aggregate>
                    </div>
                </div>
                {
                    subDivisionField && (
                        <div>
                            <h2 class='component-title'>{subDivisionLabel}</h2>
                            <div class='h-[400px]'>
                                <gs-aggregate
                                    fields={JSON.stringify([subDivisionField])}
                                    filter={JSON.stringify(lapisLocation)}
                                    headline=''
                                    size={sizeAttribute}
                                />
                            </div>
                        </div>
                    )
                }

                <div>
                    <h2 class='component-title'>Hosts</h2>
                    <div class='h-[400px]'>
                        <gs-aggregate
                            fields='["host"]'
                            filter={JSON.stringify(lapisLocation)}
                            headline=''
                            size={sizeAttribute}></gs-aggregate>
                    </div>
                </div>
            </div>
        </div>
    </gs-app>
</BaseLayout>
