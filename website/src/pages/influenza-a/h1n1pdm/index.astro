---
import { ServerSide } from '../../../views/serverSideRouting';

return Astro.redirect(
    ServerSide.routing.getOrganismView('h1n1pdm.singleVariantView').pageStateHandler.getDefaultPageUrl(),
);
---
