---
import { ServerSide } from '../../views/serverSideRouting';

return Astro.redirect(
    ServerSide.routing.getOrganismView('influenza-b.sequencingEffortsView').pageStateHandler.getDefaultPageUrl(),
);
---
